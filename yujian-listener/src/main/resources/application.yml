server:
  port: 8083

spring:
  application:
    name: yujian-listener
  profiles:
    active: test

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
